<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="mapper.academyReservation">

	<select id="sequence" resultType="int">
		select reservation_seq.nextval from dual
	</select>

	<insert id="insertReservation">
		insert into reservation
		(
			reservation_no,
			reservation_name,
			reservation_phone,
			reservation_type,
			reservation_time
		) values (
			#{reservationNo},
			#{reservationName},
			#{reservationPhone},
			#{reservationType},
			#{reservationTime}
		)
	</insert>
</mapper>