<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.exam">

    <!-- 시험 번호 시퀀스 -->
    <select id="sequence" resultType="int">
        select exam_seq.nextval
        from dual
    </select>


    <!-- 시험 등록 -->
    <insert id="insert">
        insert into exam (
            exam_no,
            course_no,
            employee_no,
            exam_title,
            exam_info,
            exam_start,
            exam_end,
            exam_limit,
            exam_status,
            exam_wtime
        )
        values (
            #{examNo},
            #{courseNo},
            #{employeeNo},
            #{examTitle},
            #{examInfo},
            #{examStart},
            #{examEnd},
            #{examLimit},
            #{examStatus},
            systimestamp
        )
    </insert>


    <!-- 시험 단일 조회 -->
    <select id="selectOne" resultType="ExamDto">
        select *
        from exam
        where exam_no = #{examNo}
    </select>


    <!-- 전체 시험 목록 조회 -->
    <select id="selectList" resultType="ExamDto">
        select *
        from exam
        order by exam_wtime desc
    </select>


    <!-- 특정 강의의 시험 목록 조회 -->
    <select id="selectListByCourse" resultType="ExamDto">
        select *
        from exam
        where course_no = #{courseNo}
        order by exam_wtime desc
    </select>


    <!-- 특정 강사가 등록한 시험 목록 조회 -->
    <select id="selectListByEmployee" resultType="ExamDto">
        select *
        from exam
        where employee_no = #{employeeNo}
        order by exam_wtime desc
    </select>


    <!-- 시험 수정 -->
    <update id="update">
        update exam
        set
            course_no = #{courseNo},
            exam_title = #{examTitle},
            exam_info = #{examInfo},
            exam_start = #{examStart},
            exam_end = #{examEnd},
            exam_limit = #{examLimit},
            exam_status = #{examStatus}
        where exam_no = #{examNo}
    </update>


    <!-- 시험 삭제 -->
    <delete id="delete">
        delete from exam
        where exam_no = #{examNo}
    </delete>

</mapper>