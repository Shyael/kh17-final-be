<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.tutorSubject">

	<!-- 강사과목 번호 생성 -->
	<select id="sequence" resultType="int">
		select tutor_subject_seq.nextval from dual
	</select>

	<!-- 강사과목 등록 -->
	<insert id="insert">
		insert into tutor_subject(
			tutor_subject_no,
			tutor_no,
			academy_subject_no
		)
		values(
			#{tutorSubjectNo},
			#{tutorNo},
			#{academySubjectNo}
		)
	</insert>

	<!-- 강사별 과목 목록 -->
	<select id="selectList" resultType="TutorSubjectDto">
		select *
		from tutor_subject
		where tutor_no = #{tutorNo}
		order by tutor_subject_no asc
	</select>

	<!-- 강사과목 상세 -->
	<select id="selectOne" resultType="TutorSubjectDto">
		select *
		from tutor_subject
		where tutor_subject_no = #{tutorSubjectNo}
	</select>

	<!-- 강사과목 수정 -->
	<update id="update">
		update tutor_subject
		set
			academy_subject_no = #{academySubjectNo}
		where tutor_subject_no = #{tutorSubjectNo}
	</update>

	<!-- 강사과목 삭제 -->
	<delete id="delete">
		delete from tutor_subject
		where tutor_subject_no = #{tutorSubjectNo}
	</delete>

</mapper>
